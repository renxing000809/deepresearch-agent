# DeepResearch 实现目的与决策依据

本文档记录每一项架构和功能改造的实现目的，避免“为了增加技术而增加技术”。后续每次开发必须先说明为什么需要实现，再进行代码修改，并在完成后记录实际收益和验证结果。

## 1. 总体目标

当前项目已经具备基础闭环：

```text
Vue 3 -> FastAPI -> Tokeness LLM -> Tavily -> Tokeness LLM -> SSE -> Vue 3
```

当前定位：

```text
LLM 驱动的线性研究流水线
```

目标定位：

```text
稳定、可恢复、可扩展、可观测的生产级 Deep Research Agent
```

所有后续设计都围绕以下问题展开：

1. 如何降低 LLM 超时和上下文过载。
2. 如何提高搜索结果质量和来源可信度。
3. 如何让单个任务失败不影响整个研究。
4. 如何在 SSE 断线、进程重启后恢复研究。
5. 如何减少重复搜索和重复网页处理。
6. 如何让报告中的事实和引用可追溯。
7. 如何量化 Token、延迟、成本和研究质量。

## 2. 实现原则

### 2.1 先解决真实问题

只有在当前架构存在明确问题时才引入新组件。例如：

- 上下文过长，才引入摘要和上下文预算。
- 重复搜索明显，才引入 Redis 查询缓存。
- 研究需要跨请求恢复，才引入 `research_id` 和状态持久化。
- 单一搜索服务不稳定，才引入 Provider 降级。

### 2.2 先简单后复杂

实施顺序遵循：

```text
结构化数据
  -> 任务级摘要
  -> 状态管理
  -> Redis 缓存
  -> 搜索降级和质量过滤
  -> 反思循环
  -> 长期记忆
```

不在基础数据流还不稳定时提前引入复杂 Agent、向量数据库或大规模并发。

### 2.3 每项改造必须可验证

每个功能都必须有：

- 明确要解决的问题
- 预期收益
- 影响范围
- 验收标准
- 测试或指标
- 未解决风险

## 3. 已完成改造的目的

### 3.1 使用 OpenAI 兼容 SDK 调用 Tokeness

实现内容：

```python
AsyncOpenAI(
    api_key=...,
    base_url="https://n.tokeness.dev/v1",
)
```

为什么需要实现：

- Tokeness 官方使用 OpenAI SDK 兼容接口。
- SDK 能统一处理请求、响应和异常类型。
- 手动拼接 HTTP 请求容易漏掉 SDK 兼容细节。
- 可以明确识别 `APITimeoutError` 等异常。

解决的问题：

- LLM 请求方式与服务商官方示例不一致。
- 请求错误难以分类。
- 超时异常捕获不完整。

预期收益：

- 提高 Tokeness 兼容性。
- 统一 LLM 调用入口。
- 便于后续替换其他 OpenAI-compatible 服务。

### 3.2 增加日志

实现内容：

- LLM 请求开始、结束和耗时日志。
- Tavily 请求开始、结束和耗时日志。
- 研究阶段日志。
- 错误类型和异常堆栈日志。
- 配置缺失日志。

为什么需要实现：

“无法连接后端”无法判断究竟是：

- FastAPI 没启动。
- 前端代理失败。
- Tokeness 超时。
- Tavily 超时。
- API Key 错误。
- 模型名称错误。
- 报告阶段输入过长。

预期收益：

- 可以定位失败阶段。
- 可以区分网络问题和业务问题。
- 可以测量每个阶段耗时。
- 不需要通过猜测排查问题。

安全约束：

- 不记录 API Key。
- 不记录 Authorization Header。
- 不默认记录完整网页正文。
- 不把敏感信息发送到前端。

### 3.3 增加超时、重试和 SSE 心跳

为什么需要实现：

深度研究不是一次请求，而是多个上游请求的组合：

```text
规划 LLM
  -> 多次 Tavily
  -> 多次任务总结 LLM
  -> 报告 LLM
```

任意一步响应较慢，都可能让用户误以为后端断开。

预期收益：

- 临时网络错误可以自动恢复。
- 上游响应慢时 SSE 连接保持活跃。
- 前端能够看到“仍在处理”而不是错误断开。
- 错误可以显示具体阶段。

当前限制：

- 仍缺少完整的研究总超时。
- 当前重试策略还需要按 HTTP 状态码进一步细分。

### 3.4 实现任务级 SummarizerAgent

原流程：

```text
多个任务搜索
  -> 所有原始结果拼接
  -> 一次性生成报告
```

新流程：

```text
任务 1 搜索 -> 任务 1 总结
任务 2 搜索 -> 任务 2 总结
任务 3 搜索 -> 任务 3 总结
  -> 最终报告
```

为什么需要实现：

- 原始网页内容直接进入最终报告会造成上下文膨胀。
- 上下文过长会增加 Token 消耗和响应时间。
- 单次报告调用失败时无法定位具体任务。
- README 的 TODO 驱动设计要求每个任务独立总结。

预期收益：

- 降低最终报告输入长度。
- 降低报告阶段超时概率。
- 每个任务产生独立中间结果。
- 后续可以缓存和恢复任务摘要。
- 方便前端显示每个任务状态。

### 3.5 拆分服务层

实现内容：

```text
services/
├── llm.py
├── search.py
├── planner.py
├── summarizer.py
└── reporter.py
```

为什么需要实现：

原来的 `main.py` 同时处理：

- API 路由
- LLM 调用
- Tavily 调用
- Prompt
- SSE
- 研究编排
- 错误处理

这会导致修改一个功能时容易影响其他功能。

预期收益：

- 每个模块职责清晰。
- 可以单独 Mock 和测试。
- 后续增加 Redis、降级和过滤时修改范围更小。
- 可以替换单个 Provider 而不重写整个系统。

### 3.6 增加结构化数据模型

实现内容：

```text
ResearchTask
SearchSource
TaskFact
TaskSummary
ResearchEvent
```

为什么需要实现：

普通字典无法保证字段完整和类型一致，尤其是 LLM 返回的数据具有不确定性。

预期收益：

- 严格校验 Planner 输出。
- 统一 Agent 之间的输入输出。
- 降低字段缺失和类型错误。
- 为 Redis、数据库和状态机提供稳定数据结构。

### 3.7 增加来源 ID 和引用校验

实现内容：

```text
S001
S002
S003
```

为什么需要实现：

普通的 `[1]`、`[2]` 引用在不同任务之间容易产生歧义，模型也可能生成不存在的引用。

预期收益：

- 任务摘要和最终报告使用同一来源注册表。
- 引用可以追溯到真实 URL。
- 可以检查模型是否引用不存在的来源。
- 为来源质量和引用覆盖率统计提供基础。

### 3.8 增加任务级 SSE 事件

实现内容：

```text
task_started
search_started
search_completed
summary_started
summary_completed
task_completed
report_completed
```

为什么需要实现：

只显示一个总进度无法让用户知道系统正在做什么，也无法定位卡在哪个任务。

预期收益：

- 前端可以显示任务列表。
- 前端可以显示完整进度日志。
- 用户可以区分搜索、总结和报告阶段。
- 后续可以支持单任务重试。

## 4. 下一阶段改造的目的

### 4.1 增加研究总超时

要解决的问题：

当前只有单次 LLM 或 Tavily 请求超时，没有整个研究流程的时间上限。

为什么需要实现：

多个任务和多次重试叠加后，研究可能运行很长时间。单次请求都没有超时，并不代表整个研究不会无限等待。

建议配置：

```env
RESEARCH_TOTAL_TIMEOUT=600
TASK_TIMEOUT=120
```

预期收益：

- 防止研究任务无限运行。
- 控制用户等待时间和 API 成本。
- 为后台 Worker 和任务取消提供边界。

验收标准：

- 超过总时间后研究进入 `FAILED` 或 `CANCELLED`。
- 当前 HTTP 客户端和 asyncio 任务被正确释放。
- 前端收到明确的总超时事件。

### 4.2 增加任务级失败隔离

要解决的问题：

当前任意任务失败可能终止整个研究。

为什么需要实现：

外部搜索服务天然不稳定，一个任务失败不意味着其他任务没有价值。

目标行为：

```text
任务 1：completed
任务 2：failed
任务 3：completed
任务 4：completed
  -> 报告标注任务 2 资料缺失
```

预期收益：

- 提高整体研究成功率。
- 保留部分可用结果。
- 用户知道具体缺失内容。
- 支持后续单独重试失败任务。

### 4.3 增加 `research_id` 和研究状态

要解决的问题：

当前状态只存在于一次 SSE 请求中，刷新页面或连接断开后无法恢复。

为什么需要实现：

深度研究通常耗时较长，不适合完全绑定在单个浏览器连接上。

目标 API：

```text
POST /api/research
GET  /api/research/{research_id}
GET  /api/research/{research_id}/stream
POST /api/research/{research_id}/cancel
```

预期收益：

- 支持状态查询。
- 支持 SSE 断线重连。
- 支持取消研究。
- 支持历史研究。
- 为后台 Worker 做准备。

### 4.4 引入状态机

要解决的问题：

当前状态只是普通字符串，缺少合法状态转移约束。

为什么需要实现：

任务恢复、取消、重试和并发执行都需要知道当前状态以及下一步允许做什么。

目标状态：

```text
CREATED
PLANNING
PLAN_VALIDATED
TASKS_QUEUED
SEARCHING
FILTERING
SUMMARIZING
TASK_COMPLETED
REFLECTING
REPORTING
COMPLETED
FAILED
CANCELLED
```

预期收益：

- 防止非法状态跳转。
- 支持进程重启恢复。
- 支持任务取消和重试。
- 日志可以还原完整生命周期。

### 4.5 引入 Redis 运行时状态

要解决的问题：

- 研究状态只在内存中。
- 相同搜索会重复调用。
- 任务摘要无法缓存。
- SSE 断线后没有事件历史。

为什么需要实现：

Redis 适合保存高频、临时、可重建的运行时数据。

初期只使用 Redis 保存：

```text
研究状态
任务状态
查询结果缓存
任务摘要缓存
SSE 事件缓存
```

不建议一开始把所有长期报告只存 Redis。正式报告和历史记录需要同步到数据库、文件系统或对象存储。

预期收益：

- 减少重复搜索。
- 减少重复 LLM 摘要。
- 支持状态恢复。
- 支持 SSE 事件补发。
- 降低上下文和 API 成本。

### 4.6 增加上下文预算管理

要解决的问题：

网页数量增加后，直接把全部内容放进 Prompt 会导致上下文过载。

为什么需要实现：

模型上下文窗口和用户预算都是有限的，不能依赖“尽量多塞资料”来提升研究质量。

目标策略：

```text
去重
  -> 删除低质量来源
  -> 删除低相关来源
  -> 用来源摘要替代正文
  -> 用任务摘要替代原始结果
```

预期收益：

- 支持处理更多网页。
- 降低报告 LLM 超时。
- 降低 Token 消耗。
- 提高模型对关键信息的注意力。

### 4.7 搜索 Provider 抽象和三级降级

要解决的问题：

当前主要依赖 Tavily，单个搜索服务不可用时研究容易失败。

为什么需要实现：

搜索服务可能受到网络、额度、限流和临时故障影响，生产系统不能假设单一 Provider 永远可用。

目标链路：

```text
Tavily -> DuckDuckGo -> Baidu
```

顺序应根据中文/英文主题、质量和实际可用性评测调整。

预期收益：

- 提高搜索成功率。
- 平衡搜索质量和资源消耗。
- 降低单一服务商故障影响。
- 记录每次降级原因。

### 4.8 细化重试和指数退避

要解决的问题：

所有异常不能使用同一种重试策略。

为什么需要实现：

- 超时和 5xx 通常可以重试。
- 401 和 403 重试没有意义。
- 429 应尊重 `Retry-After`。
- 404 通常代表地址或配置错误。

目标策略：

```python
delay = min(
    base_delay * (2 ** attempt) + random_jitter,
    max_delay,
)
```

预期收益：

- 避免短时间内重复冲击上游。
- 提高临时故障恢复率。
- 减少无意义重试和成本浪费。

### 4.9 网页质量过滤

要解决的问题：

搜索结果可能包含广告页、聚合页、重复转载、登录页、低相关内容和垃圾 SEO 页面。

为什么需要实现：

低质量来源会浪费 Token，并降低报告可信度。过滤应该在网页进入 LLM 上下文之前完成。

目标流程：

```text
URL 规范化
  -> 页面类型过滤
  -> 相关性评分
  -> 权威性评分
  -> 内容完整性评分
  -> 时效性评分
  -> 内容去重
```

预期收益：

- 减少无效上下文。
- 提高报告来源质量。
- 降低重复信息。
- 提高 LLM 总结效率。

“过滤 60% 劣质网页”只能作为待验证目标，不能当成固定系统保证。

### 4.10 ReflectionAgent 和补充检索

要解决的问题：

一次规划和一次搜索不一定覆盖完整主题。

为什么需要实现：

高质量研究需要判断当前资料是否足够，而不是固定执行完任务就生成报告。

目标输出：

```json
{
  "sufficient": false,
  "missing_aspects": ["缺少官方数据"],
  "follow_up_queries": ["... official statistics"],
  "confidence": 0.68
}
```

预期收益：

- 发现知识缺口。
- 补充关键来源。
- 检查来源冲突。
- 提升报告完整性。

约束：

- 最大循环次数。
- 最大总 Token。
- 最大总搜索次数。
- 相同查询不能重复执行。
- 没有新增信息时强制进入报告阶段。

### 4.11 长期记忆

要解决的问题：

不同研究之间无法复用已验证的来源、查询和主题摘要。

为什么需要实现：

稳定知识和来源质量不应该每次从零开始判断，跨任务复用可以降低延迟和成本。

记忆类型：

```text
来源质量记忆
主题知识记忆
查询质量记忆
```

预期收益：

- 复用高质量来源。
- 改善后续 Planner 查询。
- 减少重复搜索。
- 逐步积累领域知识。

前置条件：

- 来源注册表稳定。
- 任务摘要结构稳定。
- 质量评分可信。
- 敏感数据隔离策略明确。

### 4.12 后台 Worker

要解决的问题：

长时间研究不应依赖单个 FastAPI 请求生命周期。

为什么需要实现：

- 浏览器连接可能断开。
- 反向代理可能有请求时长限制。
- FastAPI 进程重启时内存任务会丢失。
- 研究任务需要排队和并发控制。

目标流程：

```text
POST /api/research
  -> 创建研究状态
  -> 投递 ResearchJob
  -> 返回 research_id

Worker
  -> 执行状态机
  -> 更新 Redis
  -> 发布事件
```

预期收益：

- 支持长任务。
- 支持任务排队。
- 支持重启恢复。
- 支持并发限制和取消。

## 5. 每一步的验收模板

后续实施每项功能时，必须在提交或任务完成记录中填写：

```markdown
### 功能名称

#### 为什么需要实现

说明当前问题和业务/工程原因。

#### 实现内容

说明新增模块、接口、配置和数据结构。

#### 预期收益

说明对稳定性、质量、成本或可维护性的影响。

#### 验收标准

- [ ] 标准一
- [ ] 标准二

#### 验证结果

- 测试命令：`...`
- 结果：通过 / 失败

#### 未解决问题

记录剩余风险，不把部分完成标记成全部完成。
```

## 6. 下一步执行依据

根据当前状态，下一步优先实现：

```text
增加研究总超时和任务级失败隔离，随后引入 research_id 和状态机。
```

但如果当前主要目标仍是降低 Tokeness 报告阶段超时，优先顺序可以是：

```text
任务级摘要
  -> 上下文 Token 预算
  -> 研究总超时
  -> 任务级失败隔离
```

Redis 应在结构化状态和任务级摘要稳定后接入，长期记忆应在来源质量评分和任务摘要稳定后接入。

## 7. 相关文档

```text
README.md
  -> 项目背景和基础设计

gap.md
  -> 当前实现与目标架构的差距、生产级目标设计

next_steps.md
  -> 按优先级排列的实施任务和完成状态

implementation_rationale.md
  -> 每项改造为什么需要实现，以及如何验收
```
